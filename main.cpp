#include "datastruct.h"
#include "functions.h"


int main()
{
	gameMenu();
	gameLoop();
	
    return 0;
}
/*
 * setCursor
 * system(cls)
 * getch(), mozliwe ze bedzie mozna zastapic cin>>get()
 * /

/*

    levelsHead->mRoomcontent[levelsHead->mnRooms].mItems.push_back(3);
    levelsHead->mRoomcontent[levelsHead->mnRooms].mItems.push_back(4);
    cout<<endl<<levelsHead->mRoomcontent[levelsHead->mnRooms].mItems[1];


		setCursor(0, 15);
		cout<<endl<<endl<<endl<<endl<<levelsStackHead->mnRooms<<" "<<levelsStackHead->mnLevel<<endl;
		for(int i=0; i<11; i++)
	    {
	        for(int j=0; j<11; j++)
	        {
	            cout<<levelsStackHead->mArraymap[i][j]<<" ";
	        }
	        cout<<endl;
	    }



*/
